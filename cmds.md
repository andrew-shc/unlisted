For viewing videos from SSH

Access the directory over SSH and immediately loads the video (probably because the video is short).
```sh
python3 -m http.server 8080 --directory .


conda activate relax-flow
python eval_clip_proxy.py
```