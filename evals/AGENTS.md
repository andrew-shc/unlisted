# evals/AGENTS.md

Evaluation outputs for the Neural-PBIR / nvdiffrecmc comparisons on Stanford-ORB.

This directory is currently mixed and pending a split under the new `ASSETS/`/`GREENFIELD/` conventions (not migrated yet — see root `AGENTS.md` and `BROWNFIELD.md`):
- `neural-pbir-eval-outputs.json`, `nvdiffrecmc.json`, `subset/*.json` are pure data — these belong under `ASSETS/evals/...` once the deferred data migration happens.
- `subset_sorb_eval.ipynb` is our own code — this belongs under `GREENFIELD/` once that migration happens.
